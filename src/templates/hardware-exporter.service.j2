[Unit]
Description=HTTP service for prometheus hardware exporter.

[Service]
User=root
ExecStart={{ CHARMDIR }}/venv/bin/python -m prometheus_hardware_exporter -c {{ CONFIG_FILE }}
Restart=on-failure

[Install]
WantedBy=multi-user.target
